# Copy this file to application-secret.properties before running locally.
# Keep the real password in your shell environment, not in the file.

spring.datasource.username=${MYSQL_USERNAME:root}
spring.datasource.password=${MYSQL_ROOT_PASSWORD}
